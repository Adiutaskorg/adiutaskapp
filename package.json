{
  "name": "adiutaskapp",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev -w packages/web",
    "build:web": "npm run build -w packages/web",
    "start": "bun --env-file=packages/web/.env packages/web/server/index.ts"
  }
}
